# 《曙光问道》V6.7 Closure R2.1

## 判定

`PASS`：R2.1 高密度性能门禁、资源稳定性、构建回环和启动 smoke 均通过；未进入 V6.8 视觉切片。

## FAST LOOP

命令：

```powershell
python tools/profile_high_density.py --frames 600
```

固定夹具：1440x900、确定性 seed `0x67c10`、96 个高 HP wraith、全量 Level 5 武器、8 个共鸣场、固定 `dt=1/60`、warmup 120、真实 `g.update → g.render → gsap.updateRoot`。

候选目标为连续三次 P95 `<= 15.5ms`；Run 1/2/3：`13.9ms / 14.2ms / 14.3ms`。门禁脚本在 P95 `> 16.667ms` 时返回 exit 1。

## Root cause

1. 早期长跑的主因是 transient effect 清理路径遗漏，导致 effects、scene children、geometry 和 material 持续堆积。
2. 清理路径修正后，GSAP P95 的主要残留来自每次命中都创建新的浮动伤害 DOM 节点和 tween。
3. R2.1 将浮动伤害改为 96 个元素的固定池，并把 effect/projectile/敌人/共鸣溢出路径统一到叶节点回收；慢帧 trace 继续记录 renderer 与子系统耗时，不在正常帧做 scene traversal。

## FINAL TEST

唯一终测命令：

```powershell
python tools/profile_high_density.py --frames 3000
```

结果：平均 `11.723ms`，P95 `15.4ms`，P99 `18.8ms`，最大 `34.7ms`；官方 P95 `<=16.667ms` 通过，stretch P99 `<=20ms` 通过。终测证据位于 external evidence workspace 的 `final-3000-r2.1-v2.json`。

## Resource stability

终测末态：`scene_children=449`、`geometries=1082`、唯一 `materials=506`、`effects=61`、`resonance_fields=8`、`floaters=96`。固定池数量保持稳定；无逐帧浮动伤害节点增长。

## 验证

- `python tools/build_runtime_v6_7.py`：PASS。
- `python tools/build_runtime_v6_7.py --check`：PASS，解压回环与 SHA256 相等。
- `node --check`：runtime scripts PASS。
- Browser smoke：标题 `曙光问道 · V6.7.2 启动热修`，runtime constructor 成功，Console errors `0`。
- `docs/V6.8_NOTES.md` 已从 PR #14 diff 删除。

## Scope

Browser Run 本轮不作为性能门禁阻断项；本地没有可用 Cloudflare Browser Rendering token/session，因此未进行远端 Browser Run。PR #14 只保留 V6.7 runtime closure 相关改动，不启动 V6.8 设计。
